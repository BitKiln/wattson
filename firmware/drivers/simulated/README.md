# Simulated driver

**Status: deferred to phase 2.**

An on-device synthetic source, so profiler firmware can be brought up and its USB path
load-tested before any analog hardware exists.

Not to be confused with `simulator/`, which is the host-side synthetic *device* used to develop
the PC software with no hardware at all. That one is implemented today.
